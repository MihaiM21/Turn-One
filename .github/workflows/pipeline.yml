name: Dual Environment CI/CD Pipeline

on:
  push:
    branches: [ master, dev ]

env:
  DOTNET_VERSION: '9.0.x'
  NODE_VERSION: '20'

jobs:
  build:
    name: 🏗️ Build & Test
    runs-on: ubuntu-latest
      
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: turn-one-client/package-lock.json
        
        
    - name: Restore .NET dependencies
      run: dotnet restore turn-one-backend/turn-one-backend.sln
      
    - name: Build .NET solution
      run: |
        dotnet build turn-one-backend/turn-one-backend.sln \
          --no-restore \
          --configuration Release \
          
      
    - name: Install client dependencies
      run: npm ci
      working-directory: ./turn-one-client
      
    - name: Build client application
      run: npm run build
      working-directory: ./turn-one-client
      env:
        NODE_ENV: production
        NEXT_TELEMETRY_DISABLED: 1
        
    # - name: Create deployment package
    #   run: |
    #     mkdir -p deployment-package
    #     cp -r turn-one-backend deployment-package/
    #     cp -r turn-one-client deployment-package/
    #     cp docker-compose.yml deployment-package/
    #     cp VERSION deployment-package/
    #     echo "${{ steps.environment.outputs.domain }}" > deployment-package/DOMAIN
    #     echo "${{ steps.environment.outputs.environment }}" > deployment-package/ENVIRONMENT
    #     tar -czf deployment-package-${{ steps.version.outputs.version }}.tar.gz deployment-package/
        
    # - name: Upload deployment package
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: deployment-package-${{ steps.version.outputs.version }}
    #     path: deployment-package-${{ steps.version.outputs.version }}.tar.gz
    #     retention-days: 30

  # deploy-dev:
  #   name: 🚀 Deploy to Development
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/dev'
  #   environment: 
  #     name: development
  #     url: https://dev.t1f1.com
    
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
        
  #   - name: Download deployment package
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: deployment-package-${{ needs.build.outputs.version }}
  #       path: ./
        
  #   - name: Deploy to development server
  #     run: |
  #       echo "🚀 Deploying to development server..."
  #       echo "Branch: dev"
  #       echo "Domain: dev.t1f1.com"
  #       echo "Version: ${{ needs.build.outputs.version }}"
  #       echo "Package: deployment-package-${{ needs.build.outputs.version }}.tar.gz"
        
  #       # To enable real deployment, uncomment and configure:
  #       # chmod +x scripts/deploy-vps.sh
  #       # ./scripts/deploy-vps.sh development deployment-package-${{ needs.build.outputs.version }}.tar.gz dev.t1f1.com
        
  #       echo "✅ Development deployment completed successfully!"
        
  #   - name: Health check - Development
  #     run: |
  #       echo "🏥 Running health checks for dev.t1f1.com..."
  #       # curl -f https://dev.t1f1.com/health
  #       # curl -f https://dev.t1f1.com/api/health
  #       echo "✅ Development health checks passed!"

  # deploy-production:
  #   name: 🌟 Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/master'
  #   environment: 
  #     name: production
  #     url: https://new.t1f1.com
    
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
        
  #   - name: Download deployment package
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: deployment-package-${{ needs.build.outputs.version }}
  #       path: ./
        
  #   - name: Deploy to production server
  #     run: |
  #       echo "🌟 Deploying to production server..."
  #       echo "Branch: master"
  #       echo "Domain: new.t1f1.com"
  #       echo "Version: ${{ needs.build.outputs.version }}"
  #       echo "Package: deployment-package-${{ needs.build.outputs.version }}.tar.gz"
        
  #       # To enable real deployment, uncomment and configure:
  #       # chmod +x scripts/deploy-vps.sh
  #       # ./scripts/deploy-vps.sh production deployment-package-${{ needs.build.outputs.version }}.tar.gz new.t1f1.com
        
  #       echo "✅ Production deployment completed successfully!"
        
  #   - name: Health check - Production
  #     run: |
  #       echo "🏥 Running health checks for new.t1f1.com..."
  #       # curl -f https://new.t1f1.com/health
  #       # curl -f https://new.t1f1.com/api/health
  #       echo "✅ Production health checks passed!"